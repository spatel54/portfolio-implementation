# Example Figma import context

Replace `example-slug` with a short name (e.g. `portfolio-v1`).

- **Figma URL:** (paste)
- **fileKey / nodeId:** (resolved)
- **Scope:** single-screen | …
- **Notes:** (links to related frames)
