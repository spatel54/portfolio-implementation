# Copy to .env.local for local dev. Set the same keys in Vercel (Preview + Production).

# Canonical site URL (used in app/layout.tsx metadata)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: Google Search Console
# NEXT_PUBLIC_GOOGLE_VERIFICATION=
